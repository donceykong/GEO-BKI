# Conv-BKI experiment config: KITTI-360 seq 0009, ID CENet (cenet_kitti360_softmax).

experiment_name: kitti360_seq0009_id

dataset: kitti360
sequence_name: 2013_05_28_drive_0009_sync
data_root: null

input_data_suffix: velodyne_points/data
lidar_pose_suffix: velodyne_poses.txt
input_label_suffix: inferred_labels/cenet_kitti360_softmax
gt_label_suffix: gt_labels

inferred_labels_key: kitti360
gt_labels_key: kitti360

calibration_file: ""

staging_root: baselines/convbki/staging/kitti360_seq0009_id
output_root: baselines/convbki/nbki_runs/kitti360_seq0009_id
log_dir: baselines/convbki/logs

weights:
  dir: ConvBKI_PC_02_V
  epoch: 1

grid:
  grid_size: [501, 501, 27]
  min_bound: [-50.1, -50.1, -2.7]
  max_bound: [50.1, 50.1, 2.7]

num_classes: 20
hard_input: true   # Pretrained ConvBKI_PC_02_V was trained with hard labels
filter_size: 5
kernel: sparse
per_class: true
compound: false
ell: 0.5
seed: 42
device: cuda
