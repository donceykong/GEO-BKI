# Conv-BKI experiment config: KITTI-360 seq 0009, OOD CENet (cenet_mcd_softmax).

experiment_name: kitti360_seq0009_ood

dataset: kitti360
sequence_name: 2013_05_28_drive_0009_sync
data_root: null

input_data_suffix: velodyne_points/data
lidar_pose_suffix: velodyne_poses.txt
input_label_suffix: inferred_labels/cenet_mcd_softmax
gt_label_suffix: gt_labels

inferred_labels_key: mcd
gt_labels_key: kitti360

calibration_file: ""

staging_root: baselines/convbki/staging/kitti360_seq0009_ood
output_root: baselines/convbki/nbki_runs/kitti360_seq0009_ood
log_dir: baselines/convbki/logs

weights:
  dir: ConvBKI_PC_02_V
  epoch: 1

grid:
  grid_size: [1000, 1000, 27]
  min_bound: [-100.0, -100.0, -2.7]
  max_bound: [100.0, 100.0, 2.7]

num_classes: 20
filter_size: 5
kernel: sparse
per_class: true
compound: false
ell: 0.5
seed: 42
device: cuda
